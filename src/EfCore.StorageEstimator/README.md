# EfCore.StorageEstimator

This project contains the library source for `EfCore.StorageEstimator`.

The runtime API estimates PostgreSQL heap and secondary-index storage from EF Core models plus annotation-driven planning assumptions. It also renders structured reports for Markdown and JSON consumers.

Use the root [README.md](../../README.md) for repository-level usage and development notes.
